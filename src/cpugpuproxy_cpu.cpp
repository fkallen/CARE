#include <cpugpuproxy.hpp>

#include <config.hpp>

#include <vector>

namespace care{

    std::vector<int> getUsableDeviceIds(std::vector<int> deviceIds){
        deviceIds.clear();

        return deviceIds;
    }

}
